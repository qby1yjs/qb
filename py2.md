### 1. 代数证明题

**推导过程：**

一元线性回归模型为：
$$ y_i = \beta_0 + \beta_1 x_i + \varepsilon_i $$
其最小二乘估计（OLS）的拟合值为：
$$ \hat{y}_i = \hat{\beta}_0 + \hat{\beta}_1 x_i $$
残差定义为：
$$ e_i = y_i - \hat{y}_i = y_i - (\hat{\beta}_0 + \hat{\beta}_1 x_i) $$

最小二乘法的目标是最小化残差平方和（RSS）：
$$ RSS = \sum_{i=1}^n e_i^2 = \sum_{i=1}^n (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i)^2 $$

为了求得最优的 $\hat{\beta}_0$ 和 $\hat{\beta}_1$，我们需要对 $RSS$ 分别关于 $\hat{\beta}_0$ 和 $\hat{\beta}_1$ 求偏导，并令其等于零（一阶条件）：

1. **对 $\hat{\beta}_0$ 求偏导：**
$$ \frac{\partial RSS}{\partial \hat{\beta}_0} = \sum_{i=1}^n 2(y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) \cdot (-1) = -2 \sum_{i=1}^n e_i = 0 $$
由此可得：
$$ \sum_{i=1}^n e_i = 0 $$
即残差之和必然等于零。

2. **对 $\hat{\beta}_1$ 求偏导：**
$$ \frac{\partial RSS}{\partial \hat{\beta}_1} = \sum_{i=1}^n 2(y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) \cdot (-x_i) = -2 \sum_{i=1}^n x_i e_i = 0 $$
由此可得：
$$ \sum_{i=1}^n x_i e_i = 0 $$
即残差与自变量乘积之和必然等于零。

---

### 2. 理解思考题

**解答：**

**对于普通 $R^2$：** 
普通 $R^2$ 的计算公式为 $1 - \frac{RSS}{TSS}$。在添加一个特征（无论是否随机）后，模型可以通过调整参数，至少让残差平方和（$RSS$）保持不变，甚至进一步微小下降。因为 $TSS$ 是不变的，所以 $RSS$ 不增加会导致 $R^2$ 必然上升或保持不变。

**对于调整 $R^2_{adj}$：**
调整 $R^2_{adj}$ 引入了“自由度惩罚”，其计算公式通常为 $1 - \frac{RSS / (n - p - 1)}{TSS / (n - 1)}$。当添加一个纯随机的无关特征时，它无法真正解释因变量的变异，因此 $RSS$ 几乎不会有任何实质性的下降。然而，模型参数个数（$p$）增加了 $1$，这导致分母中的自由度 $(n - p - 1)$ 变小了。
分母的自由度减小，会放大分子上的 $RSS$ 均值，从而使得整体分数变大，最终导致 $1$ 减去该分数后的 $R^2_{adj}$ 必然下降。
